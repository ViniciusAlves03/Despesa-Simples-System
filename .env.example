#################################################################################################
#######################################  GENERAL EXECUTION SETUP  ##############################
#################################################################################################

# API_HOSTNAME                  Name of the server hosting the Kong API Gateway.
#                               default: api.exp.localhost
API_HOSTNAME=api.exp.localhost

# WEB_APP_HOSTNAME              Name of the server hosting the frontend application.
#                               default: exp.localhost
WEB_APP_HOSTNAME=exp.localhost

# TZ                            Timezone for the microservices to ensure consistent time handling.
#                               default: America/Recife
TZ=America/Recife

# NODE_ENV                      The execution environment for Node.js (e.g., development, production).
#                               default: development
NODE_ENV=development

# ISSUER                        The issuer name for JWT tokens, identifying the token creator.
#                               default: exps
ISSUER=exps

# DASHBOARD_HOST                The base URL of the frontend dashboard, used for CORS or redirects.
#                               default: http://localhost:4200
DASHBOARD_HOST=http://localhost:4200

#################################################################################################
#################################### API GATEWAY (KONG) SETUP ###################################
#################################################################################################

# KONG_POSTGRES_USER            Username to connect to the Kong gateway's PostgreSQL database.
#                               default: kong
KONG_POSTGRES_USER=kong

# KONG_POSTGRES_PASS            Password to connect to the Kong gateway's PostgreSQL database.
#                               default: kong
KONG_POSTGRES_PASS=kong

# INTERNAL_GATEWAY_API_URL      The internal URL for the Kong Admin API.
#                               default: http://api-gateway:8001
INTERNAL_GATEWAY_API_URL=http://api-gateway:8001

#################################################################################################
################################# DATABASES & BROKERS SETUP #####################################
#################################################################################################

# MONGODB_URI                   The connection string for the MongoDB database.
#                               default: mongodb://mongo:27017/expensesDB
MONGODB_URI=mongodb://mongo:27017/expensesDB

# RABBITMQ_URI                  The connection string for the RabbitMQ message broker.
#                               default: amqp://rabbitmq:5672
RABBITMQ_URI=amqp://rabbitmq:5672

#################################################################################################
###################################### ACCOUNT SERVICE SETUP ####################################
#################################################################################################

# ACCOUNT_PORT_HTTP             The external HTTP port mapping for the Account service.
#                               default: 3000
ACCOUNT_PORT_HTTP=3000

# ACCOUNT_PORT_HTTPS            The external HTTPS port mapping for the Account service.
#                               default: 3001
ACCOUNT_PORT_HTTPS=3001

# JWT_SECRET                    Secret key used for signing JWT tokens. Keep this value secure.
#                               default: s1cr1tk1y
JWT_SECRET=s1cr1tk1y

# ADMIN_EMAIL                   The email for the default administrator account.
#                               default: admin@exp.com
ADMIN_EMAIL=admin@exp.com

# ADMIN_PASSWORD                The password for the default administrator account.
#                               default: admin123
ADMIN_PASSWORD=admin123

#################################################################################################
###################################### MICROSERVICES PORTS ######################################
#################################################################################################

# BUDGETS_PORT_HTTP             The external HTTP port for the Budgets service.
#                               default: 4000
BUDGETS_PORT_HTTP=4000

# BUDGETS_PORT_HTTPS            The external HTTPS port for the Budgets service.
#                               default: 4001
BUDGETS_PORT_HTTPS=4001

# CATEGORIES_PORT_HTTP          The external HTTP port for the Categories service.
#                               default: 5000
CATEGORIES_PORT_HTTP=5000

# CATEGORIES_PORT_HTTPS         The external HTTPS port for the Categories service.
#                               default: 5001
CATEGORIES_PORT_HTTPS=5001

# EXPENSES_PORT_HTTP            The external HTTP port for the Expenses service.
#                               default: 6000
EXPENSES_PORT_HTTP=6000

# EXPENSES_PORT_HTTPS           The external HTTPS port for the Expenses service.
#                               default: 6001
EXPENSES_PORT_HTTPS=6001

# INCOMES_PORT_HTTP             The external HTTP port for the Incomes service.
#                               default: 7000
INCOMES_PORT_HTTP=7000

# INCOMES_PORT_HTTPS            The external HTTPS port for the Incomes service.
#                               default: 7001
INCOMES_PORT_HTTPS=7001
